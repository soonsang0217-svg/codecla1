services:
  morning-briefing:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - briefing-data:/app/data
    restart: unless-stopped

volumes:
  briefing-data:
